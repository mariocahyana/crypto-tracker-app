package com.example.gamestorehb.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Monochrome Palette ────────────────────────────────────────────────────────
val Black = Color(0xFF000000)
val White = Color(0xFFFFFFFF)

// Dark backgrounds
val Background = Color(0xFF0A0A0A)
val Surface = Color(0xFF111111)
val SurfaceVariant = Color(0xFF1A1A1A)
val CardSurface = Color(0xFF161616)

// Borders & dividers
val Outline = Color(0xFF2A2A2A)
val OutlineVariant = Color(0xFF1F1F1F)

// Text shades
val TextPrimary = Color(0xFFF5F5F5)
val TextSecondary = Color(0xFF9E9E9E)
val TextTertiary = Color(0xFF616161)

// Accent (minimal — used for interactive elements & price indicators)
val AccentWhite = Color(0xFFE0E0E0)
val AccentGray = Color(0xFF424242)

// Semantic colors (kept desaturated for monochrome feel)
val Positive = Color(0xFF66BB6A)   // subtle green for positive change
val Negative = Color(0xFFEF5350)   // subtle red for negative change
val Neutral = Color(0xFF757575)
